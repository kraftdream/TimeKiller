using UnityEngine;
using System.Collections;

public class TimeTrackerScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void FixedUpdate () {
//	    Debug.Log("Elapsed " + Time.timeSinceLevelLoad / 1000);
	}
}
